# 工程架构目录与代码规范

最近更新：2026-06-09

## 功能定位

本文是 qnvideo-gzt 的长期工程规范和目录蓝图，用于约束后续功能开发、渐进式拆分、接口契约、数据边界、测试验收和部署风险。

本规范先建立口径，不代表立即大范围重构。后续做某个功能时，才在该功能边界内按本文逐步落地。

## 执行原则

- 规范先定，执行跟随功能推进。
- 不为了目录整齐单独跨功能搬迁大量文件。
- 每次优化一个功能时，同步检查该功能涉及的页面、组件、接口、服务、数据库字段、文件存储、测试和文档。
- 页面保持壳层，复杂状态进 `composables`，复用 UI 进 `components`，接口请求进 `api`。
- 后端 `routes` 只做鉴权、参数校验和响应封装；业务编排、模型参数、文件处理、任务状态和数据访问下沉到 `services`。
- 新增逻辑不得继续堆到已经超长的页面或 service 中，除非是临时补丁，并且必须记录后续拆分点。
- 代码改动后必须同步更新对应功能文档；方案暂不开发时，写入对应文档的“方案池 / 待确认讨论”。

## 当前项目情况

- 项目已经从上游 LocalMiniDrama 演化为用户自有的 AI 短视频创作平台。
- 当前同时包含剧本、角色、场景、道具、分镜、图片、视频、AI 配置、任务队列、素材库、权限和部署等多条业务链路。
- 前端部分核心页面承担了过多状态和业务逻辑，后续需要按功能逐步拆出组件和 composable。
- 后端模型接入、文件存储、任务队列和生成服务已经开始拆分，但仍需继续把路由、模型运行时、文件生命周期和业务编排分层。
- PostgreSQL 是正式结构化业务主库；媒体文件、生成结果和上传文件统一走 `data/storage`，数据库只保存引用和元数据。

## 目标总目录

```text
qnvideo-gzt/
  frontweb/                  前端应用
  backend-node/              后端 API、任务、AI 接入
  docs/                      产品、工程和功能档案
  data/                      本地开发持久化数据，服务器部署时必须保护
  scripts/                   项目级脚本：部署、巡检、迁移、体检
  docker-compose.codex.yml   服务器部署 compose
```

根目录不再新增零散规则文档。稳定协作规则放根目录 `AGENTS.md`；产品、功能、工程细则放 `docs/`。

## 前端目录规范

目标分层：

```text
frontweb/src/
  api/                       只放 HTTP 请求封装和接口 DTO 适配
  components/
    aiConfig/                AI 配置、模型策略、工作流配置组件
    filmCreate/              创作台业务组件
      layout/                制作页固定布局：顶部栏、左侧导航、一级标签、项目设置与一键动作
      common/                制作页真正跨工作台共用组件：预览、规格、任务状态、模型策略、工作流选择
      workbenches/           制作页业务工作台，按剧本/角色/道具/场景/分镜/视频聚合
    mediaLibrary/            媒体素材库组件
    common/                  通用业务组件
  composables/
    aiConfig/                AI 配置状态、模型策略、用量统计
    filmCreate/              创作台业务状态和动作编排
    tasks/                   任务同步、轮询、取消、恢复
    storage/                 文件引用、上传、预览状态
  stores/                    Pinia 全局状态
  utils/                     纯工具函数
  views/                     页面壳层，只做路由入口和区域编排
```

前端页面规则：

- `views` 不承载复杂生成流程和大段业务判断。
- `api` 不写 UI 状态，不直接操作 store。
- 制作页拆分以“业务工作台”为第一边界，不以零散 UI 控件为第一边界；剧本、角色、道具、场景、分镜、视频合成各自保持完整工作台语义。
- `common` 只放真正跨多个工作台复用且语义稳定的公共能力，例如图片预览、规格弹窗、模型策略标签、工作流选择器、任务状态；不得为了看起来细而把每个按钮、输入框、小布局提前拆成公共组件。
- `FilmCreate.vue` 长期目标是制作工作台壳层，只保留路由、项目级状态装配、顶部项目设置和一级标签页编排；剧本、角色、道具、场景、分镜、视频合成必须拆到 `components/filmCreate/workbenches/` 对应业务工作台目录。
- 制作页顶部固定保留项目整体设置和“本次生成模型策略”栏；模型策略栏只做本次生成的文本/图片/视频/音频路由覆盖选择，默认自动，不替代管理员 AI 配置。
- 超过 800 行的组件，新增逻辑优先拆到同功能组件或 composable。
- 超过 1500 行的组件，本功能改动必须记录拆分方案或暂缓理由。
- 超过 3000 行的组件，原则上不继续堆新主逻辑。

### FilmCreate 拆分蓝图

制作工作台后续按“顶部常驻 + 一级标签页”拆分，不再把所有功能纵向堆在一个页面中。拆分后的理解方式是：一个业务工作台就是一个相对完整的创作房间，工作台内部可以有自己的列表、表单、弹窗、局部工具栏和业务状态；只有多个工作台反复共用且含义一致的能力，才进入 `common`。

目标组件：

```text
frontweb/src/views/FilmCreate.vue                 页面壳层：路由、项目装配、顶层工作台编排
frontweb/src/components/filmCreate/layout/
  FilmCreateHeader.vue                            顶部栏：返回、项目标题、集数选择、AI 配置入口
  QuickNav.vue                                    左侧快捷导航：剧本、素材、分镜、视频等定位入口
  FilmWorkbenchTabs.vue                           一级工作台 Tab：剧本/角色/场景/道具/分镜/视频
  ProjectPipelinePanel.vue                        项目整体设置、一键动作、模型策略、流水线状态
frontweb/src/components/filmCreate/common/
  ImagePreviewOverlay.vue                         公共图片放大预览、左右切换，仅用于跨工作台统一预览体验
  ImageSpecDialog.vue                             公共图像规格设置，仅用于多个工作台共用同一规格编辑规则
  WorkflowPresetSelect.vue                        公共生成规范选择，仅用于角色/场景/道具/分镜等同类选择入口
  TaskStatusBadge.vue                             公共任务状态展示，仅用于统一任务视觉状态
  ModelStrategyChips.vue                          公共文本/图片/视频/音频模型策略标签，仅用于顶部或跨工作台展示
frontweb/src/components/filmCreate/workbenches/
  script/
    ScriptWorkbench.vue                           剧本工作台：故事生成、分集剧本、小说导入、剧本库选择
    ScriptDialogs.vue                             剧本相关弹窗：导入小说、从剧本库导入
  characters/
    CharacterWorkbench.vue                        角色工作台：提取角色、添加角色、角色库、角色图生成、上传、工作流选择
    CharacterDialogs.vue                          角色相关弹窗：编辑角色、角色库、SD2 认证
  props/
    PropWorkbench.vue                             道具工作台：提取道具、添加道具、道具库、道具图生成、上传、工作流选择
    PropDialogs.vue                               道具相关弹窗：编辑道具、道具库
  scenes/
    SceneWorkbench.vue                            场景工作台：提取场景、添加场景、场景库、场景图生成、上传、工作流选择
    SceneDialogs.vue                              场景相关弹窗：编辑场景、场景库
  storyboards/
    StoryboardWorkbench.vue                       分镜工作台：分镜生成、编辑、首尾帧、全能模式、分镜图和分镜视频
    StoryboardDialogs.vue                         分镜相关弹窗：提示词、首尾帧提示词、视频参数
  video/
    VideoWorkbench.vue                            视频合成工作台：字幕、水印、旁白/对白音轨、整集合成、导出预览
```

配套 composable：

```text
frontweb/src/composables/filmCreate/
  useFilmCreateProject.js                         项目加载、项目设置、当前集、整体工作台状态
  useWorkbenchLoader.js                           首屏摘要与各工作台按需加载
  useAiRouteSelection.js                          文本/图片/视频/音频模型策略选择和 payload
  useWorkflowPresets.js                           角色/场景/道具/分镜生成规范选择
  useScriptWorkbench.js                           剧本工作台逻辑
  useCharacterWorkbench.js                        角色工作台逻辑
  usePropWorkbench.js                             道具工作台逻辑
  useSceneWorkbench.js                            场景工作台逻辑
  useStoryboardWorkbench.js                       分镜工作台逻辑
  useVideoWorkbench.js                            视频合成工作台逻辑
  usePipelineRunner.js                            一键生成素材及分镜文本、一键成片、暂停/重试/倒计时
frontweb/src/utils/filmCreate/
  mediaSpec.js                                    图片规格、视频规格、画幅比例换算
  promptFormat.js                                 图片/视频/首尾帧提示词格式化
  storyboardMedia.js                              分镜图片/视频引用、首尾帧、参考图排序
  srt.js                                          字幕 SRT 时间轴格式化
```

拆分顺序：

1. 先把当前平铺的 `tabs/*.vue` 迁移为 `workbenches/<业务>/...Workbench.vue`，保持功能不变，只完成目录达标。
2. 再按工作台拆弹窗：剧本弹窗进 `ScriptDialogs.vue`，角色/道具/场景弹窗分别进对应 `Dialogs.vue`，分镜弹窗进 `StoryboardDialogs.vue`。
3. 再按工作台下沉 composable：先项目加载、AI 路由、工作流规范，再剧本/角色/道具/场景，最后分镜和一键流程。
4. 单个工作台文件超过 800-1200 行时，再在该工作台文件夹内继续拆局部组件；不提前建立过细组件目录。
5. 如果某个局部组件只被一个工作台使用，优先留在该工作台文件夹内，不进入 `common`。

一级标签顺序建议为：`剧本`、`角色`、`场景`、`道具`、`分镜`、`视频/合成`。如果用户后续坚持“角色、道具、场景”顺序，资源 tab 可调整，但拆分边界不变。

### 制作工作台懒加载与聚合接口

制作页性能优化不能只靠拆组件。后续 `/film/:id` 必须按“首屏轻摘要 + 标签页按需加载 + 后端聚合视图模型”的方式演进：

- 打开项目时只请求工作台摘要，不拉取全量剧本、资源、分镜、图片历史和视频历史。
- 摘要接口由后端聚合项目基础设置、一键动作状态、本次模型策略摘要、各标签计数和运行任务数。
- 用户第一次打开某个标签页时，再请求该标签页的完整视图数据。
- 已加载标签页在前端内存缓存；用户点击刷新、生成任务完成、项目设置变化或摘要版本变化时再刷新。
- 生成任务轮询按当前标签页收敛；离开标签页后暂停高频轮询，只保留低频摘要刷新。
- 后端返回缩略图和稳定文件引用，前端不得在列表中请求原图、base64 或完整视频正文。
- 后端 tab 接口返回前端可直接渲染的 view model，不让前端串联多个接口再自行拼装权限、任务、文件、模型路由和引用状态。
- `storyboards` 与 `video-compose` 工作台接口只返回分镜轻量字段、主图/首尾帧引用和视频/合成摘要；媒体历史、关键帧历史、base64 图片和完整视频正文必须通过专门媒体接口按需加载。

首屏摘要接口：

```text
GET /api/v1/workbench/projects/:drama_id/summary
```

响应字段口径：

```json
{
  "project": {
    "id": 1,
    "title": "项目标题",
    "cover_url": "/static-thumb/320/...",
    "updated_at": "2026-06-09T00:00:00.000Z"
  },
  "settings": {
    "aspect_ratio": "9:16",
    "media_spec": "2K · 9:16 · 1080x1920",
    "video_resolution": "720P",
    "default_segment_duration": 15,
    "language": "zh-CN",
    "style": "sci-fi"
  },
  "generation_strategy": {
    "text": { "mode": "auto", "label": "自动" },
    "image": { "mode": "auto", "label": "自动" },
    "video": { "mode": "unset", "label": "未配置" },
    "audio": { "mode": "auto", "label": "自动" }
  },
  "tabs": {
    "script": { "ready": true, "count": 1, "version": "..." },
    "characters": { "count": 8, "running_tasks": 1, "version": "..." },
    "scenes": { "count": 5, "running_tasks": 0, "version": "..." },
    "props": { "count": 12, "running_tasks": 0, "version": "..." },
    "storyboards": { "count": 40, "running_tasks": 3, "version": "..." },
    "videoCompose": { "count": 1, "running_tasks": 1, "version": "..." }
  }
}
```

标签页视图接口：

```text
GET /api/v1/workbench/projects/:drama_id/tabs/script
GET /api/v1/workbench/projects/:drama_id/tabs/assets?type=character
GET /api/v1/workbench/projects/:drama_id/tabs/assets?type=scene
GET /api/v1/workbench/projects/:drama_id/tabs/assets?type=prop
GET /api/v1/workbench/projects/:drama_id/tabs/storyboards?episode_id=:episode_id
GET /api/v1/workbench/projects/:drama_id/tabs/video-compose?episode_id=:episode_id
```

接口职责：

- `summary` 只服务首屏和全局状态刷新，响应要轻。
- `script` 返回剧本/分集摘要、当前剧集文本和剧本导入必要状态。
- `assets` 返回对应角色、场景或道具列表、主图缩略图、生成状态、关联分镜数量、可用动作和当前工作流规范。
- `storyboards` 返回当前集分镜列表、镜头字段、轻量参考图、任务状态和分镜工作流规范，不混入资源管理列表的全量卡片。
- `video-compose` 返回当前集视频片段、合成记录、缺失项、字幕/音频摘要和可执行动作。

阶段性落地允许旧接口继续存在，但新增制作页逻辑优先调用 workbench 聚合接口；旧的 `drama.getDrama()` 全量加载只能作为兼容或显式刷新使用。

2026-06-09 基础落地状态：

- 已新增 `backend-node/src/routes/workbench.js` 与 `backend-node/src/services/workbenchService.js`，提供摘要、剧本、资产、分镜、视频/合成基础 tab view model。
- 已让 `FilmCreate.vue` 打开项目时优先加载工作台摘要和当前 tab；角色、场景、道具、分镜、视频/合成首次进入时再加载。
- 已完成制作页主要 UI 模板组件拆分，并已按“业务工作台聚合”迁移目录：布局组件位于 `components/filmCreate/layout/`；剧本、角色、道具、场景、分镜、视频分别位于 `components/filmCreate/workbenches/script|characters|props|scenes|storyboards|video/`。
- 已按工作台聚合口径拆出弹窗层：`ScriptDialogs.vue`、`CharacterDialogs.vue`、`PropDialogs.vue`、`SceneDialogs.vue`、`StoryboardDialogs.vue` 分别留在对应工作台目录；图片规格、AI 配置、用量中心、工作流配置和图片预览保留在 `common/FilmCreateCommonDialogs.vue`；资源参考图隐藏输入保留为 `common/ResourceRefImageInputs.vue`。
- 已开始下沉顶层状态和纯工具：`useAiRouteSelection.js` 管理本次生成模型策略、运行时模型路由、payload 和路由策略并发；`useWorkflowPresets.js` 管理角色/场景/道具/分镜生成规范选择；`utils/filmCreate/mediaSpec.js` 管理图片/视频规格、比例换算和归一化。
- 当前拆分仍是“模板先行、核心业务状态暂留父页”的过渡方案，`FilmCreate.vue` 通过显式 `proxyRefs` 上下文桥接把现有状态与函数提供给子组件；下一阶段要继续把项目装配、一键流程、任务轮询、分镜媒体同步和各工作台业务函数下沉到 `composables/filmCreate/`。
- 分镜 tab 当前仍会按分镜列表再请求图片/视频媒体历史；这是为了兼容现有分镜卡片，后续继续把轻量参考图和任务状态并入后端 view model。
- 合成 tab 当前加载当前集分镜、视频生成记录和合成记录，不再触发整项目全量 `loadDrama()`。
- 旧 `loadDrama()` 仍保留给生成完成、兼容刷新和高风险状态恢复使用；后续按功能逐步改为“当前 tab 刷新 + 摘要刷新”。
- 为避免拆出子组件后 scoped 样式丢失，已抽出 `frontweb/src/styles/filmCreateShared.css`，由 `FilmCreate.vue` 一次性加载；共享样式中的 scoped-only 选择器已转为普通 CSS 选择器，避免多个子组件重复打包。后续应按组件归属继续细分局部样式，降低全局选择器范围。

## 后端目录规范

目标分层：

```text
backend-node/src/
  routes/                    薄路由：鉴权、参数校验、响应封装
  services/
    ai-runtime/              模型选择、路由、额度、用量、健康状态
    ai-text/                 文本模型协议、调用和解析
    ai-image/                图片模型协议、参考图、落盘和结果适配
    ai-video/                视频模型协议、任务查询、视频落盘
    ai-audio/                TTS 和音频模型协议
    workflow-presets/        工作流规范、提示词渲染、版本快照
    writing/                 故事、剧本、小说导入、分集文本和编剧结构
    storyboards/             分镜生成、镜头字段、首尾帧、连续性
    resources/               角色、场景、道具、素材引用
    storage/                 路径、元数据、缩略图、引用、清理、回收站
    tasks/                   任务状态机、队列、取消、恢复
    auth/                    账号、权限、身份上下文
  db/                        数据库适配、schema、迁移辅助
  migrations/                数据库迁移
  utils/                     纯工具函数
  test/                      按模块组织的测试
```

后端规则：

- `routes` 不直接拼复杂 prompt、不直接落盘、不直接调多个模型服务。
- AI 协议差异放在对应 `ai-*` adapter 中，不散落在业务 service。
- 任务状态、模型调用、文件存储必须可追踪，必要时用 `trace_id` 串联。
- 文件生成必须先落盘，再把稳定的 `local_path` / `/static/...` 写回业务对象。
- 业务 service 可以编排多个模块，但不应承担底层协议和文件生命周期细节。

## 共享契约与枚举

后续需要逐步建立共享契约目录，避免前后端字段各写一套：

```text
shared/
  contracts/
    aiConfig.schema.json
    workflowPreset.schema.json
    storyboard.schema.json
    asset.schema.json
    task.schema.json
  enums/
    serviceTypes.json
    taskStatus.json
    workflowPresetTypes.json
    assetTypes.json
```

短期内可以先在文档中固定字段口径，再按模块重构时落成 schema 或类型文件。

## API 与响应规范

- 接口字段命名保持一致，避免同一概念出现 `type`、`preset_type`、`mode` 混用。
- 新接口需要有明确的请求参数、响应结构、错误码和权限边界。
- AI 配置、工作流规范、素材、任务、分镜、文件引用都必须支持管理员和普通用户权限差异。
- 长耗时生成动作必须走任务或可查询状态，不做无法恢复的黑盒请求。
- 外部模型错误要保留可排查信息，但最终响应不得暴露 API Key、代理、完整密钥或敏感请求头。

## 数据库与字段规范

- PostgreSQL 是正式结构化业务主库。
- 历史 SQLite 只作为迁移和兼容来源，不作为正式业务库。
- 表结构新增必须通过 migration，不能只依赖运行时补列。
- 所有业务核心表应明确 `created_by` / `owner_user_id` / `project_id` / `deleted_at` 等归属和软删除字段。
- JSON 字段只存适合半结构化扩展的数据；稳定查询字段应提升为显式列。
- 版本化对象需要保存版本号或快照，保证历史任务可追溯。

## 文件存储边界

文件存储生命周期由 `docs/14---文件存储生命周期与清理.md` 统一约束。本文只保留工程边界：

- 媒体正文不写入 PostgreSQL。
- 数据库保存 `local_path`、展示 URL、MIME、大小、尺寸、来源、任务、项目、创建人和引用关系。
- `data/storage` 是持久化目录，部署、拉代码、容器重建不得覆盖。
- 清理必须先引用检查，再软删除或进入回收站；物理删除需要 dry-run。

## 模型基础设施边界

模型配置、路由、协议和运行策略是项目基础设施，不应散落在具体业务页面中：

- `docs/02---AI模型配置与全局路由.md` 管模型配置、协议规范、路由、失败切换、冷却和运行策略。
- `docs/12---模型用量统计与额度治理.md` 管用量、额度、成本、周期、限制和告警。
- 文本、图片、视频、音频模型按类型拆分 adapter。
- 特殊中转接口只在协议适配层处理，业务层只关心统一输入和统一结果。

## 日志与可观测性边界

日志和审计由 `docs/13---日志审计与可观测性.md` 统一约束。本文只规定开发要求：

- 重要生成链路要能查任务、查模型调用、查文件落盘、查失败原因。
- 管理员操作、配置变更、额度策略、删除动作需要审计。
- 日志不得打印密钥、token、代理订阅、完整授权头。

## 测试、部署与验收

- 后端改动优先运行 `backend-node` 的相关测试，必要时运行 `npm test`。
- 前端改动优先运行对应测试，涉及页面结构时运行 `npm run build`。
- 数据库、权限、上传、生成链路、部署配置改动，最终说明必须写清已测项和未测风险。
- 部署和服务器验证遵守根目录 `AGENTS.md`，不在文档任务中主动连接服务器。

## 当前工程风险摘要

以下是 2026-06-09 当前重点风险文件的稳定摘要，只用于指导渐进式拆分，不作为每次都要维护的全量行数清单。

| 文件 | 当前约行数 | 风险级别 | 处理口径 |
| --- | ---: | --- | --- |
| `frontweb/src/views/FilmCreate.vue` | 7492 | 极高 | 已拆出 Header、QuickNav、项目设置与一键流程、一级工作台、剧本/角色/道具/场景/分镜/视频合成 UI 模板、主要弹窗、AI 路由选择、工作流规范选择和媒体规格工具；当前行数仍高主要因为父页保留项目装配、生成编排、媒体同步、任务轮询和上下文桥接。下一步把项目装配、一键流程、任务轮询和分镜/视频核心状态继续下沉 composables。 |
| `backend-node/src/services/videoClient.js` | 3508 | 极高 | 已拆出 `ai-video` 协议/解析基础层，后续继续拆厂商 adapter、任务查询和结果落盘。 |
| `frontweb/src/components/AIConfigContent.vue` | 2848 | 高 | 已拆出运行观测和全局策略组件，后续继续拆模型列表、配置表单和测试连接。 |
| `backend-node/src/services/imageService.js` | 1874 | 高 | 后续做图片链路时拆出业务编排、参考图、结果导入和任务状态。 |
| `backend-node/src/services/episodeStoryboardService.js` | 1690 | 高 | 后续做分镜时拆出导演参数、镜头字段、连续性和首尾帧策略。 |
| `backend-node/src/routes/storyboards.js` | 1124 | 高 | 后续做分镜接口时保持薄路由，把编排下沉 service。 |
| `backend-node/src/services/aiConfigService.js` | 914 | 中 | 已拆出运行策略服务，后续继续拆模型能力、配置校验和连接测试。 |
| `backend-node/src/services/imageClient.js` | 615 | 已降级 | 已作为图片模型门面，继续保持协议差异下沉到 `ai-image`。 |

历史全量扫描如果需要保留，应作为单独审计快照或体检报告，不再放入本文主规范，避免过期数据污染开发口径。

## 模块执行规则

后续按功能执行时遵循以下方式：

- 做模型配置：先读 `docs/02`、`docs/12`、本文，再拆 `AIConfigContent.vue`、`aiConfigService.js`、模型 adapter 和测试；新增页面优先落到独立组件或 `components/aiConfig/`。
- 做工作流规范：先读 `docs/15`、`docs/05`、`docs/07`，再拆工作流配置组件、选择器和后端服务。
- 做素材库：先读 `docs/05`、`docs/06`、`docs/14`，再拆资源 UI、素材接口、文件引用和清理策略。
- 做分镜导演台：先读 `docs/07`、`docs/08`、`docs/09`、本文，再拆分镜 UI、生成参数、首尾帧和任务状态。
- 做任务队列：先读 `docs/10`、`docs/12`、`docs/13`，再拆任务状态机、模型调用记录和前端任务中心。
- 做部署运维：先读根目录 `AGENTS.md`、`docs/17`，再执行服务器操作。

## 待开发清单

- 建立共享契约或 schema，先覆盖 AI 配置、任务、素材、工作流和分镜。
- 将 `FilmCreate.vue` 拆为页面壳层、资源管理、分镜导演台、一键动作和任务进度模块。
- 制作页已具备“项目整体设置 + 本次生成模型策略 + 一级标签页”的基础结构，且主要工作台 UI 与弹窗层已按业务目录聚合；后续拆项目装配、一键动作、任务轮询和各工作台状态。
- 将 `AIConfigContent.vue` 拆为模型配置、全局策略、工作流规范、用量额度和测试连接模块。
- 继续将 `videoClient.js` 拆成 `ai-video` 厂商 adapter、任务查询和结果落盘模块。
- 建立统一 `storage/` 服务，处理路径、元数据、缩略图、引用和清理。
- 建立 `ai-runtime/`，集中处理模型选择、用量、额度、健康状态和路由策略。

## 验收清单

- 新功能落位符合本文目录边界。
- 页面、组件、接口、service、数据库字段和文档同步更新。
- 超长文件没有继续堆新增主逻辑，或已记录暂缓理由和拆分点。
- 路由保持薄层，业务编排和协议适配没有散落。
- 文件正文不进数据库，生成结果有稳定本地引用。
- 任务、模型调用、文件落盘和错误原因可追踪。
- 已说明相关测试、构建、部署或未测风险。

## 更新记录

- 2026-06-09：瘦身工程规范，移除过时全量行数清单，稳定“规范先定、按模块执行”的口径。
- 2026-06-09：制作工作台摘要与基础标签页懒加载落地；新增 workbench 后端路由/service，前端打开项目优先加载摘要和当前 tab。
- 2026-06-09：制作页主要 UI 模板完成首轮拆分，已拆出 Header、QuickNav、项目设置与一键动作、工作台 tabs、剧本、角色、道具、场景、分镜、视频/合成组件；业务状态暂留父页作为过渡。
- 2026-06-09：制作页弹窗层按工作台聚合拆分，剧本/角色/道具/场景/分镜弹窗分别进入对应工作台目录，公共图片规格、AI 配置、用量中心、工作流配置和图片预览进入 `common`。
- 2026-06-09：制作页顶层模型策略、生成规范和媒体规格开始下沉，新增 `useAiRouteSelection.js`、`useWorkflowPresets.js`、`utils/filmCreate/mediaSpec.js`。
- 2026-06-09：确认 `FilmCreate.vue` 下一阶段拆分方向：顶部项目设置与本次生成模型策略常驻，主体按剧本、角色、场景、道具、分镜、视频/合成标签页拆分。
- 2026-06-09：补充项目目录规范化蓝图、超长文件处理规则和模型/文件/日志边界。
- 2026-06-09：模型运行观测按独立组件和 `ai-runtime` 查询服务落地，避免继续向 `AIConfigContent.vue` 堆主逻辑。
- 2026-06-09：模型配置第一步收口，全局路由策略前端拆为独立组件，后端拆出 `routingPolicyService`。
- 2026-06-09：模型配置第三步收口，业务场景 key 由 `modelSceneKeys` 统一维护，前端场景映射改为读取后端定义，图片/TTS/部分文本入口开始按场景路由。
- 2026-06-09：视频模型第四步开始拆分，新增 `ai-video/videoProtocol.js` 与 `videoResultParser.js`，先把协议判断、URL 构建、模型选择和返回解析移出超长文件。
- 2026-06-09：模型用量第五步落地，新增 `ai_usage_events` 和 `usageTracker`，调用日志与正式用量账本分层维护。
- 2026-06-09：额度治理第六步落地，新增 `ai_quota_policies`、`ai_quota_counters` 与 `quotaGuard`，运行时模型候选开始执行超额过滤。
- 2026-06-09：日志 trace 第七步落地，模型调用日志与用量事件统一写入 `trace_id`，查询接口支持 trace 过滤。
- 2026-06-09：第八步在制作页一键动作区展示模型策略摘要，尽量只补入口提示，不扩散改动到一键流程主逻辑。
- 2026-06-08：建立工程架构目录与代码规范初版。
