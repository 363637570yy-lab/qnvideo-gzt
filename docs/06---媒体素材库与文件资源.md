# 媒体素材库与文件资源

最近更新：2026-06-09

## 功能定位

媒体素材库管理用户可见、可筛选、可预览、可选择、可删除或可加入项目的媒体资源，包括上传素材、生成图片、生成视频、音频和可复用文件引用。

本文关注“素材库页面和业务资源行为”。底层文件目录、落盘规则、引用检查、回收站、孤儿文件和清理策略统一放在 `14---文件存储生命周期与清理.md`。

## 当前项目情况

- 媒体素材库入口是 `/media-library`。
- 当前页面支持全部/图片/视频筛选、关键词搜索、分页、上传进度、预览、选择和批量删除。
- 素材主表是 `assets`，保存名称、类型、分类、URL、本地路径、大小、MIME、宽高、时长、来源生成记录和创建人。
- 上传和生成文件统一应落在 `data/storage`，PostgreSQL 只保存引用。
- 普通用户只能删除自己创建的素材，管理员不受此限制。

## 当前已落地能力

- 支持图片、视频等素材列表。
- 支持上传素材、预览素材、删除素材。
- 支持素材创建人记录。
- 支持从图片生成结果或视频生成结果导入素材。
- 服务器持久化目录为 `/opt/ai-video/qnvideo-gzt/data`，项目内媒体统一使用 `data/storage`。

## 关联代码

- 前端页面：`frontweb/src/views/MediaLibrary.vue`
- 前端上传接口：`frontweb/src/api/upload.js`
- 前端素材接口：`frontweb/src/api/images.js`、`frontweb/src/api/videos.js`
- 后端上传：`backend-node/src/routes/upload.js`、`backend-node/src/services/uploadService.js`
- 后端素材：`backend-node/src/routes/assets.js`、`backend-node/src/services/assetService.js`
- 存储清理：`backend-node/src/services/storageCleanupService.js`
- 测试：`backend-node/test/storageCleanup.test.js`、`backend-node/test/assetPermissions.test.js`

## 当前问题与不足

- 素材库与角色/场景/道具库之间的边界需要更明确。
- 删除素材时，对已有项目、分镜、生成记录的引用影响需要更可见。
- 文件清理、回收站和孤儿文件扫描由 `14---文件存储生命周期与清理.md` 统一约束，素材库只呈现可删除判断和引用详情。

## 方案池 / 待确认讨论

- 是否把素材分为原始上传、生成结果、精选素材、项目引用四类。
- 是否增加“被哪些项目/分镜引用”的反查。
- 是否增加素材归档而不是直接删除。

## 待开发清单

- 梳理所有本地文件路径字段。
- 梳理删除素材时的引用保护策略。
- 增加素材列表筛选：创建人、来源、类型、项目引用、生成任务。
- 与文件存储生命周期文档对齐，前端只展示“可删除 / 不可删除 / 已归档 / 已进入回收站”等业务状态。

## 验收清单

- 媒体文件不写入 PostgreSQL。
- 删除素材不会误删仍被项目引用的关键文件。
- 普通用户只能编辑或删除自己创建的素材。

## 更新记录

- 2026-06-09：明确本文只负责媒体素材库的用户侧行为，底层文件生命周期移交 `docs/14`。
- 2026-06-08：建立媒体素材档案初版。
