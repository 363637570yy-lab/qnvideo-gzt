# 项目列表与项目整体设置

最近更新：2026-06-10

## 功能定位

项目是所有剧本、角色、场景、道具、分镜、图片、视频和合成结果的业务容器。项目整体设置负责给后续生成链路提供默认规格。

## 当前项目情况

- 项目列表入口是 `/`，项目详情入口是 `/drama/:id`，制作入口是 `/film/:id`。
- 项目结构由 `dramas` 和 `episodes` 承载，项目下挂角色、场景、道具、分镜、图片、视频和合成记录。
- 项目详情页已展示剧集信息、分集列表、剧内资源库和制作资源。
- 制作页已能保存故事梗概、分集剧本、项目进度和部分媒体规格。
- 当前应把画幅、画风、默认语言、默认片段时长继续收敛为项目整体设置。

## 当前已落地能力

- 项目列表页展示用户可访问的项目。
- 支持创建、导入、导出、删除项目。
- 项目支持标题、简介、类型、风格、总集数等信息。
- 项目整体设置已明确应包含画幅、主画风、默认语言、默认每段时长。
- 制作页项目整体设置状态已收敛到 `useProjectSettings.js`，包括画幅、主画风、默认语言、默认时长、图像规格、视频规格和延迟保存。
- 制作页顶部项目整体设置已常驻显示，切换角色、道具、场景、分镜和视频工作台时仍保留在顶部。
- 制作页顶部以轻提示条说明“修改已实时保存，再次生成素材、分镜或视频时将按当前设置生效。”，避免用户误以为已生成内容会自动重算。
- 制作页顶部已展示本次生成模型策略摘要，用于提示文本、图片、视频、音频等当前路由状态；左侧导航不再放模型下拉选择。
- 制作页左侧导航的阶段数量和完成态改为优先读取工作台摘要接口 `progress_steps`，不再依赖懒加载后的本地数组推断；有当前集时摘要接口带 `episode_id`，分镜脚本、分镜图和分镜视频按当前集统计。
- 普通用户只能访问自己的项目，管理员可查看全部项目。

## 关联代码

- 前端页面：`frontweb/src/views/FilmList.vue`
- 项目详情：`frontweb/src/views/DramaDetail.vue`
- 制作页面：`frontweb/src/views/FilmCreate.vue`
- 制作页项目设置状态：`frontweb/src/features/filmCreate/shared/composables/useProjectSettings.js`
- 制作页顶部控制区：`frontweb/src/features/filmCreate/layout/ProjectPipelinePanel.vue`
- 制作页导航进度：`frontweb/src/features/filmCreate/layout/useQuickNavProgress.js`
- 前端接口：`frontweb/src/api/drama.js`
- 工作台摘要接口：`frontweb/src/api/workbench.js`
- 后端路由：`backend-node/src/routes/drama.js`
- 后端工作台摘要：`backend-node/src/services/workbenchService.js`
- 后端服务：`backend-node/src/services/dramaService.js`
- 媒体规格：`backend-node/src/services/projectMediaSpec.js`
- 初始表结构：`backend-node/migrations/01_init.sql`

## 当前问题与不足

- 项目整体设置和单次分镜生成设置容易混在一起。
- 项目整体设置、管理员 AI 配置、本次生成模型策略三者已经初步视觉隔离；后续仍需把“本次指定模型”的入口和权限口径继续产品化。
- 制作页已开始改为“轻量摘要首屏 + 标签页懒加载”：首屏使用工作台摘要接口，剧本、角色、场景、道具、分镜、视频/合成按标签页加载；后续还要继续拆组件并减少旧 `loadDrama()` 兼容刷新。
- 项目创建时没有足够强的创作规格确认，导致后续角色、场景、分镜图、视频生成容易各自为政。
- 项目列表与制作链路之间缺少“当前项目完成度 / 风险提示 / 下一步动作”的引导。

## 方案池 / 待确认讨论

- 项目创建流程是否拆成“基础信息 + 视觉规格 + 生成策略”三步。
- 项目卡片是否显示剧本、资产、分镜、图片、视频、合成状态。
- 项目整体设置是否应支持锁定，锁定后批量生成必须使用同一画幅和主画风。
- 制作页顶部固定为“项目整体设置 + 一键动作 + 本次生成模型策略”三行结构，并作为制作页常驻控制区：
  - 项目整体设置：画幅、图像规格、视频清晰度、默认时长、默认语言、主画风。
  - 一键动作：一键生成素材及分镜文本、一键成片（测试中慎用）。
  - 本次生成模型策略：文本、图像、视频、音频按“自动 / 指定配置 / 未配置”显示摘要，并提供刷新和管理员配置入口。
- 本次生成模型策略只影响当前制作页后续生成动作的路由覆盖，默认自动；它不替代管理员 AI 配置，也不应写回全局模型配置。
- 本次生成模型策略建议以轻量筛选框或标签按钮展示：`文本 自动/指定`、`图像 自动/指定`、`视频 自动/指定`、`音频 自动/指定`。未配置时必须显式提示，避免黑盒失败。
- 制作页首屏应优先调用 `GET /api/v1/workbench/projects/:drama_id/summary`，只拿项目整体设置、本次模型策略摘要、一键动作状态和各标签计数/任务数；具体剧本、资源、分镜、视频数据在用户打开对应标签页后再加载。
- 工作台摘要接口必须提供 `progress_steps`：故事剧本、角色、道具、场景、分镜脚本、分镜图、分镜视频分别返回 `status`、`count`、`total_count`、`ready_count`、`running_tasks`，供左侧导航直接消费；请求可携带 `episode_id` 来返回当前集范围的剧本、分镜图和视频进度。

## 待开发清单

- 明确项目整体设置字段和数据库落点。
- 明确本次生成模型策略字段是否仅存在前端状态，还是保存为项目/用户偏好；默认建议先作为制作页会话级状态。
- 给项目列表增加创作进度摘要。
- 继续把制作页顶部项目整体设置与工作流规范、单次分镜生成设置做更清晰的边界提示。
- 继续减少制作页生成完成后的全量刷新，把刷新范围收敛到当前标签页或摘要。

## 验收清单

- 新建项目后，画幅、画风、语言、默认时长能被后续角色、场景、分镜和视频生成读取。
- 单次生成参数不会覆盖项目整体设置，除非用户明确保存为项目默认值。
- 本次生成模型策略显示的文本、图像、视频、音频路由与实际请求传参一致，切换后可在模型调用日志或任务诊断中追踪。
- 打开制作页时，首屏不请求全量分镜和资源历史；只有进入对应标签页后才加载该标签页完整数据。
- 左侧导航数量和完成态在未打开对应懒加载标签页时仍与后端摘要一致。
- 修改项目整体设置后，顶部常驻区域显示“修改已实时保存，再次生成素材、分镜或视频时将按当前设置生效。”。
- 普通用户无法访问他人项目。

## 更新记录

- 2026-06-10：制作页顶部项目整体设置改为常驻控制区，增加实时保存轻提示；本次生成模型摘要扩展到文本、图像、视频、音频，保留“刷新/配置”入口并移除左侧模型下拉面板；左侧导航阶段统计改为优先使用工作台摘要接口 `progress_steps`。
- 2026-06-10：制作页项目整体设置状态下沉到 `useProjectSettings.js`，为后续继续拆项目装配和减少父页状态做准备。
- 2026-06-10：制作页目录迁移到 `features/filmCreate`，项目设置状态归入制作页共享 composable。
- 2026-06-09：补充制作页顶部“本次生成模型策略”口径，明确它是当前生成动作的模型覆盖选择，不等同全局 AI 配置。
- 2026-06-09：落地制作工作台摘要接口和基础标签页懒加载；制作页打开项目时优先加载摘要和当前标签页，不再默认全量拉取资源、分镜和视频历史。
- 2026-06-09：补充制作页首屏摘要和标签页懒加载口径，要求后端提供聚合 view model，提升打开项目速度。
- 2026-06-08：建立项目整体设置档案初版。
