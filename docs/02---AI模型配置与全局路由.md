# AI模型配置与全局路由

最近更新：2026-06-08

## 功能定位

AI 配置是系统的模型能力中心，负责管理文本、图像、视频、音频等服务商配置，并为不同业务场景提供可控、可切换、可降级的模型路由。

## 当前项目情况

- AI 配置入口是 `/ai-config`，仅管理员可访问。
- 配置主表是 `ai_service_configs`，业务场景映射表是 `ai_model_map`。
- 管理页已分为 AI 配置、提示词高级设置、业务场景高级设置、SD2 资产管理。
- 当前支持文本/对话、图像、视频、TTS、即梦2角色认证等配置类型。
- 当前已支持全局路由策略：优先级、并发上限、请求超时、视频等待、重试次数、冷却时间。
- 当前视频协议已覆盖 OpenAI 兼容、Sora 中转、Veo3、火山 Seedance、Seedance 全能、DashScope、Vidu、Jimeng 自建等方向。

## 当前已落地能力

- AI 配置仅管理员可访问。
- 支持文本、图像、视频、音频/TTS 等服务类型。
- 支持多个配置按排序使用。
- 支持运行时公开读取当前模型路由。
- 支持失败计数、冷却、禁用状态、最后错误等字段。
- 支持部分服务商连接测试和素材资产查询。

## 关联代码

- 前端页面：`frontweb/src/views/AiConfig.vue`
- 前端组件：`frontweb/src/components/AIConfigContent.vue`
- 前端接口：`frontweb/src/api/ai.js`
- 后端路由：`backend-node/src/routes/aiConfig.js`
- 配置服务：`backend-node/src/services/aiConfigService.js`
- 文本客户端：`backend-node/src/services/aiClient.js`
- 图片客户端：`backend-node/src/services/imageClient.js`
- 视频客户端：`backend-node/src/services/videoClient.js`
- 路由映射：`backend-node/src/routes/sceneModelMap.js`
- 迁移：`backend-node/migrations/14_storyboard_segments_and_model_map.sql`、`backend-node/migrations/25_ai_routing_and_media_specs.sql`

## 当前问题与不足

- “自动选择模型”的业务规则需要更明确：按服务类型、业务场景、项目规格、模型能力还是成本排序。
- 不同模型的能力边界需要落档，例如是否支持首尾帧、参考图数量、TTS、官方异步任务。
- 模型失败后的降级、重试、冷却、提示用户策略还需要更统一。
- 当前配置管理偏技术表单，缺少面向创作流程的说明和风险提示。

## 方案池 / 待确认讨论

- 引入模型能力标签：文生图、图生图、首尾帧、全能视频、TTS、多参考图、长视频、同步/异步。
- 引入业务场景路由：故事生成、角色提取、分镜生成、提示词润色、角色图、分镜图、分镜视频、TTS。
- 引入成本策略：优先质量、优先速度、优先低成本、备用路线。
- 管理员是否可以为某个项目单独指定模型策略。

## 待开发清单

- 梳理每个生成入口使用的 `scene_key` 或业务路由键。
- 补充模型能力字段或配置约定。
- 给用户侧生成入口显示当前将使用的模型策略摘要。

## 验收清单

- 管理员配置多个同类模型后，系统能按明确顺序选择。
- 单个配置失败时，错误能记录，并按规则切换或提示。
- 普通用户无法进入 AI 配置页面和管理接口。

## 更新记录

- 2026-06-08：建立 AI 模型配置档案初版。
